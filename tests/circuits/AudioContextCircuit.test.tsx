import React, { useContext, useState, createContext } from "react";
import { Tone } from "@components/circuits/TypeCircuit";
import { useAudioContextCircuit } from "@components/circuits/AudioContextCircuit";

describe("AudioContextCircuit", () => {
    it("2 +2=4", () => {
        expect(2 + 2).toBe(4);
    });
});